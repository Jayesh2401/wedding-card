# Run locally

From this directory, start a static web server:

```bash
python3 -m http.server 8000
```

Then open <http://127.0.0.1:8000/>.

The page is a recovered copy of the original Tilda site. Its local downloaded files
are grouped under `assets/`; some page resources are still loaded from Tilda's CDN.
